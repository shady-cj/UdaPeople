- name: copy the artifacts to the remote server
  copy:
    src: "~/poject/artifact.tar.gz"
    dest: ~/

- name:
  shell: |
    cd ~/
    ls
    tar -zxf artifact.tar.gz
    npm install
    pm2 stop default
    pm2 start npm -- start
